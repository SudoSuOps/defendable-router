{
  "name": "defendable-router",
  "version": "0.1.0",
  "private": true,
  "description": "DefendableRouter — we cracked the router. OpenWrt for AI agents. Drop-in middleware that intercepts agent traffic and writes receipts. Write-only · no blocking · <5ms POST. Free OSS install · paid receipts/grading/insurance.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
